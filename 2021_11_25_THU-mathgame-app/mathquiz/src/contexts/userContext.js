//TODO: for creating context, you have to import createContext first.
import React, { createContext } from "react";

//TODO: second, you have to declare on variable and assign createContext() . it makes a context
export const userContext = createContext();

// export default userContext;
